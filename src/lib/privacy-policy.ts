// 개인정보 처리방침 버전 관리.
// 이 상수를 올리면(예: 'v2' → 'v3') 기존 동의자에게도 재동의 모달이 다시 노출된다(강제 재동의).
// 값을 올릴 때는 반드시 /privacy 페이지(src/app/privacy/page.tsx)와 동의 모달(PrivacyConsentModal)의
// 표기(버전·시행일)를 함께 갱신할 것.
export const PRIVACY_POLICY_VERSION = 'v2'
export const PRIVACY_POLICY_EFFECTIVE_DATE = '2026-06-20'
